export class User{
    constructor(public userId: number, public name: string, public employeeId: number, 
        public projectName: string, public taskName: string, public userStatus: number){
    }
}