export class Project{
    constructor(public projectId: number, public projectName: string, public priority: number, 
        public startDate: Date, public endDate: Date, public projectStatus: number)
        {

        }
}