import { FilterTaskPipe } from './filter-task.pipe';

describe('FilterTaskPipe test', () => {
  it('create an FilterTaskPipe instance', () => {
    const pipe = new FilterTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
